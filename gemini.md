# 👁️ KUNAYA LABS: PSY-PROFILER OS — MASTER CONTEXT
## ESSENTIALS ONLY. NO EMPIRE LOGIC. PRODUCT WING (SACHI) ONLY.

**Tech Stack:** Next.js 14 (App Router), Tailwind CSS, Framer Motion, Appwrite Cloud.

**The Aesthetic (Sachi Paradigm):**
- You are building the digital facade of Sachi (CEO of Product). 
- Aesthetic: Classified Military Intelligence meets Cyberpunk.
- Colors: Deep Black (`bg-[#04040A]`), Crimson Alerts (`text-[#E94560]`), Sachi Gold (`text-[#C9A84C]`).
- Typography: Use `font-mono` for all classified data, badges, and IDs. 
- Vibe: Dark, sleek, no white backgrounds. Frame things like a confidential dossier.

**Backend Rule (Appwrite):**
- All database and auth interactions MUST use the client initialized in `src/lib/appwrite.ts`.

**The Intelligence Architecture (For Context):**
The backend profiling pipeline uses 7 agents (NEXUS, INTAKE, OCEAN, SHADOW, BOND, ARCHETYPE, DRIVE, ASIX). Any frontend UI that displays reports must map to these 7 divisions. 

**Strict Output Rules:**
- Output complete code. No placeholders like `// add logic here`.
- Always implement responsive design (mobile-first Tailwind).
- Always include loading states and error handling for Appwrite calls.